import { makeStyles, Theme } from "@material-ui/core";

const options = {
  name: "auth",
};

const useStyles = makeStyles(
  (theme: Theme) => ({
    root: {},
  }),
  options
);

export default useStyles;
